/*global jQuery, document, edd_wallet_vars*/
jQuery(document).ready(function ($) {
    'use strict';

    // Remove the discounts form
    $('#edd_discount_code').remove();

    // Remove the software licensing renewal form
    $('#edd_sl_renewal_form').remove();
});
